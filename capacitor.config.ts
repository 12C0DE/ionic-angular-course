import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ionic.angular.course.12h',
  appName: 'ionic-angular-course',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
